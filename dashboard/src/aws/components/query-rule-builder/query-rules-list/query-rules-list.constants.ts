/*
 * SPDX-FileCopyrightText: 2024-2026 Espressif Systems (Shanghai) CO LTD
 *
 * SPDX-License-Identifier: Apache-2.0
 */

/**
 * Boolean field whose `true`/`false` reads as Online/Offline rather than the
 * generic True/False rendering used for every other boolean field.
 */
export const ONLINE_STATUS_FIELD = "shadow.name.iparams.reported.online";
